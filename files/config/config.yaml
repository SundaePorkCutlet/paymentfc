app:
  port: 28083

database:
  host: postgres-payment
  port: 5432
  user: admin
  password: admin
  name: payment

redis:
  host: redis
  port: 6379
  password: admin

secret:
  jwt_secret: secret301

kafka:
  broker: localhost:29093
  topics:
    - order.created: order.created
    - payment.success: payment.success
  group_id: payment-service

xendit:
  secret_api_key: ""
  webhook_token: ""

mongo:
  uri: mongodb://admin:admin@localhost:27017
  database: payment_audit

# true: order.created 시 인보이스 직접 생성 안 함 → payment_requests 저장만, 배치에서 인보이스 생성
# false: order.created 시 바로 CreateInvoice (실시간)
toggle:
  disable_create_invoice_directly: true
